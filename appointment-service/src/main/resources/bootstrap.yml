spring:
  application:
    name: appointment-service
  cloud:
    config:
      uri: ${CONFIG_URI:http://localhost:8888}
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
