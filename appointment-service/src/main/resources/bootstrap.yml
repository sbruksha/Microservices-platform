spring:
  application:
    name: appointment-service
  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: true
      password: eodessa
      username: user
      discovery:
        enabled: true
        serviceId: config-server
      profile: default